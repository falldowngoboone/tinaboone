{
  "name": "tinaboone.com",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:falldowngoboone/tinaboone.com.git",
  "author": "Ryan Boone <falldowngoboone@gmail.com>",
  "license": "MIT",
  "scripts": {
    "browsersync": "browser-sync start --server --files '**/*.html, css/**/*.css, js/**/*.js, images/**/*'",
    "sass": "node-sass --watch css -o css",
    "start": "run-p sass browsersync",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "browser-sync": "^2.23.6",
    "node-sass": "^4.8.3",
    "npm-run-all": "^4.1.2"
  }
}
